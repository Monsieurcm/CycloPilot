# API Configuration
API_PORT=4000
API_HOST=localhost

# Web Configuration
WEB_PORT=3000
WEB_HOST=localhost

# Database (future)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cyclopilot
DB_USER=postgres
DB_PASSWORD=changeme

# Environment
NODE_ENV=development
